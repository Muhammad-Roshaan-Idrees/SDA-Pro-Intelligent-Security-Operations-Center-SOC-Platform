(Minimal README

Quick start (dashboard only):

1. Install dependencies and start dashboard:

```bash
cd soc-dashboard
npm install
npm start
```

2. Open demo UI: http://localhost:3002

Run integration test:

```bash
cd soc-dashboard
npm test
```

Run with Docker Compose (builds images and runs services):

```bash
docker compose up --build
```

)
